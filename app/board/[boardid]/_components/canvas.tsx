"use client";

export const Canvas = () => {
    return (
        <main className="h-full w-full relative bg-neutral-100 touch-none">
            canvas!
        </main>
    )
};

export default Canvas;